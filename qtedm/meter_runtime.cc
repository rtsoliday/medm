/*
 * All implementation logic has been moved to the template base class
 * SingleChannelMonitorRuntimeBase<MeterElement>. This file exists
 * solely for build system compatibility and may be removed once the
 * Makefile is updated to handle header-only runtime classes.
 *
 * See single_channel_monitor_runtime_base.{h,cc} for the actual implementation.
 */

#include "meter_runtime.h"
